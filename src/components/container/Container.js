import React , {Component, Fragment } from 'react';

class Container extends Component {

    constructor(...args){
      super(...args);
    }
    render() {
      return (
        <section>
           主体组件
        </section>
      );
    }
}

export default Container;
